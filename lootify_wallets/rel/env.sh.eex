#!/bin/sh

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=wallets@wallets.lootify.local
export RELEASE_COOKIE="${ERLANG_COOKIE:-lootify_secret_cookie}"
export ERL_EPMD_ADDRESS=0.0.0.0
export ERL_AFLAGS="-kernel inet_dist_listen_min 9000 inet_dist_listen_max 9010"
